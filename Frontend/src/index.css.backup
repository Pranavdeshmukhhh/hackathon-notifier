@import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

* { box-sizing: border-box; margin: 0; padding: 0; }
body { font-family: 'Inter', sans-serif; background-color: #fafafa; color: #334155; line-height: 1.6; }
a { color: inherit; text-decoration: none; }
.app-container { min-height: 100vh; }

/* ── NAVBAR ── */
.navbar {
  position: sticky;
  top: 0;
  z-index: 100;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid #e2e8f0;
  padding: 0 2rem;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.navbar-brand {
  display: flex;
  align-items: center;
  gap: 10px;
  font-weight: 800;
  font-size: 1.25rem;
  color: #0f172a;
  letter-spacing: -0.5px;
  flex-shrink: 0;
}
.brand-icon {
  width: 34px;
  height: 34px;
  background: #10b981;
  border-radius: 8px;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  font-weight: 800;
  font-size: 1rem;
  flex-shrink: 0;
}
.navbar-links {
  display: flex;
  align-items: center;
  gap: 1.5rem;
}
.navbar-links a {
  font-size: 0.9rem;
  font-weight: 600;
  color: #475569;
  transition: color 0.2s;
  padding: 6px 0;
  white-space: nowrap;
}
.navbar-links a:hover,
.navbar-links a.nav-active { color: #0f172a; }
.navbar-links a.nav-active { border-bottom: 2px solid #10b981; padding-bottom: 4px; }

/* Hamburger button */
.mobile-menu-btn {
  display: none;
  background: none;
  border: none;
  cursor: pointer;
  padding: 8px;
  flex-shrink: 0;
}
.hamburger {
  display: block;
  width: 22px;
  height: 2px;
  background: #0f172a;
  position: relative;
  transition: all 0.3s;
}
.hamburger::before,
.hamburger::after {
  content: '';
  position: absolute;
  width: 22px;
  height: 2px;
  background: #0f172a;
  transition: all 0.3s;
}
.hamburger::before { top: -7px; }
.hamburger::after  { top:  7px; }
.hamburger.open { background: transparent; }
.hamburger.open::before { top: 0; transform: rotate(45deg); }
.hamburger.open::after  { top: 0; transform: rotate(-45deg); }

/* ── BUTTONS ── */
.btn-primary {
  background: #0f172a;
  color: white;
  padding: 10px 24px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 0.9rem;
  transition: all 0.2s;
  border: none;
  cursor: pointer;
  white-space: nowrap;
}
.btn-primary:hover { background: #1e293b; transform: translateY(-1px); }
.btn-secondary {
  background: white;
  color: #475569;
  padding: 10px 20px;
  border-radius: 8px;
  font-weight: 600;
  font-size: 0.9rem;
  transition: all 0.2s;
  border: 1px solid #e2e8f0;
  cursor: pointer;
  white-space: nowrap;
}
.btn-secondary:hover,
.btn-secondary.active { background: #f8fafc; color: #0f172a; border-color: #cbd5e1; }

/* ── HERO ── */
.hero {
  padding: 5rem 2rem 3rem;
  max-width: 1200px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  gap: 4rem;
  flex-wrap: wrap;
}
.hero-content { flex: 1; min-width: 0; }
.hero-content h1 {
  font-size: 3.5rem;
  font-weight: 900;
  letter-spacing: -2px;
  line-height: 1.1;
  color: #0f172a;
  margin-bottom: 1.25rem;
}
.hero-content p {
  font-size: 1.15rem;
  color: #475569;
  line-height: 1.6;
  margin-bottom: 2rem;
  max-width: 500px;
}
.hero-cta-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 1rem;
}
.hero-stats-panel {
  flex: 1;
  min-width: 0;
  background: white;
  border: 1px solid #e2e8f0;
  border-radius: 16px;
  padding: 2rem;
  box-shadow: 0 20px 40px -15px rgba(0,0,0,0.05);
}
.hero-stats-header {
  display: flex;
  align-items: center;
  gap: 8px;
  margin-bottom: 1.5rem;
  font-weight: 700;
  color: #0f172a;
}
.hero-stats-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
.hero-stat-box {
  background: #f8fafc;
  border: 1px solid #f1f5f9;
  padding: 1.25rem;
  border-radius: 12px;
}
.hero-stat-value {
  font-size: 1.75rem;
  font-weight: 800;
  color: #10b981;
  margin-bottom: 4px;
  word-break: break-word;
}
.hero-stat-label {
  font-size: 0.75rem;
  font-weight: 700;
  color: #64748b;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* ── COLD START BANNER ── */
.cold-start-banner {
  background: #fffbeb;
  border: 1px solid #fde68a;
  border-radius: 10px;
  padding: 14px 20px;
  margin: 1rem 2rem;
  display: flex;
  align-items: center;
  gap: 12px;
  font-size: 0.9rem;
  color: #92400e;
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
}
.cold-start-spinner {
  width: 18px;
  height: 18px;
  border: 2px solid #fde68a;
  border-top-color: #d97706;
  border-radius: 50%;
  animation: spin 0.8s linear infinite;
  flex-shrink: 0;
}
@keyframes spin { to { transform: rotate(360deg); } }

/* ── LOGO STRIP ── */
.logo-strip {
  border-top: 1px solid #e2e8f0;
  border-bottom: 1px solid #e2e8f0;
  background: white;
  padding: 2rem;
  text-align: center;
}
.logo-strip p {
  font-size: 0.9rem;
  font-weight: 700;
  color: #64748b;
  margin-bottom: 1.5rem;
  text-transform: uppercase;
  letter-spacing: 1px;
}
.logo-grid {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 2.5rem;
  flex-wrap: wrap;
}
.logo-item {
  font-size: 1.2rem;
  font-weight: 800;
  color: #cbd5e1;
  letter-spacing: -0.5px;
  display: flex;
  align-items: center;
  gap: 8px;
  filter: grayscale(100%);
  transition: color 0.3s;
}
.logo-item:hover { color: #94a3b8; }

/* ── SECTION HEADERS ── */
.section-header {
  padding: 4rem 2rem 1.5rem;
  max-width: 1200px;
  margin: 0 auto;
  border-bottom: 1px solid #f1f5f9;
  margin-bottom: 2rem;
}
.section-header h2 {
  font-size: 1.75rem;
  font-weight: 800;
  color: #0f172a;
  letter-spacing: -0.5px;
  display: flex;
  align-items: center;
  gap: 10px;
}
.section-header p { color: #64748b; font-size: 1rem; margin-top: 0.5rem; }

/* ── ABOUT BENTO BOX ── */
.about-section {
  background: white;
  padding: 4rem 0;
  border-bottom: 1px solid #e2e8f0;
}
.about-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}
.about-card {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 16px;
  padding: 2rem;
  transition: transform 0.2s, box-shadow 0.2s;
}
.about-card:hover { transform: translateY(-2px); box-shadow: 0 10px 25px -5px rgba(0,0,0,0.05); }
.about-card-icon {
  font-size: 1.5rem;
  color: #10b981;
  margin-bottom: 1.25rem;
  background: #ecfdf5;
  width: 48px;
  height: 48px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 12px;
}
.about-card h3 {
  font-size: 1.15rem;
  font-weight: 800;
  color: #0f172a;
  margin-bottom: 0.75rem;
  letter-spacing: -0.3px;
}
.about-card p { font-size: 0.95rem; color: #475569; }

/* ── DASHBOARD HIGHLIGHTS ── */
.dashboard-section { padding: 4rem 0; }
.highlight-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
  gap: 1.5rem;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
}
.highlight-card {
  border-radius: 20px;
  padding: 2.5rem;
  color: white;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  min-height: 280px;
  position: relative;
  overflow: hidden;
  box-shadow: 0 10px 30px -10px rgba(0,0,0,0.1);
}
.highlight-card.bg-orange { background: linear-gradient(135deg, #f97316, #ea580c); }
.highlight-card.bg-blue   { background: linear-gradient(135deg, #0ea5e9, #0284c7); }
.highlight-card h3 {
  font-size: 1.6rem;
  font-weight: 800;
  margin-bottom: 1rem;
  line-height: 1.2;
  letter-spacing: -0.5px;
  z-index: 1;
  max-width: 90%;
}
.highlight-stats { display: flex; gap: 3rem; margin-top: auto; z-index: 1; }
.h-stat { display: flex; flex-direction: column; }
.h-stat-val { font-size: 2.75rem; font-weight: 900; line-height: 1; margin-bottom: 8px; }
.h-stat-label {
  font-size: 0.85rem;
  font-weight: 600;
  opacity: 0.9;
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

/* ── SEARCH + SORT ── */
.search-sort-container {
  background: white;
  border-top: 1px solid #e2e8f0;
  border-bottom: 1px solid #e2e8f0;
  padding: 1.5rem 0;
  position: sticky;
  top: 64px;
  z-index: 90;
}
.search-sort-bar {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 1rem;
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 2rem;
  flex-wrap: wrap;
}
.search-box {
  display: flex;
  align-items: center;
  gap: 10px;
  flex: 1;
  min-width: 220px;
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
  padding: 12px 16px;
  transition: all 0.2s;
}
.search-box:focus-within {
  background: white;
  border-color: #10b981;
  box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1);
}
.search-box svg { color: #64748b; flex-shrink: 0; }
.search-box input {
  flex: 1;
  background: none;
  border: none;
  outline: none;
  color: #0f172a;
  font-size: 0.95rem;
  font-family: inherit;
  min-width: 0;
}
.search-box input::placeholder { color: #94a3b8; }

.sort-box { display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
.sort-box label { font-size: 0.9rem; color: #475569; font-weight: 600; white-space: nowrap; }
.sort-box select {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  color: #0f172a;
  font-size: 0.9rem;
  font-weight: 600;
  padding: 10px 14px;
  cursor: pointer;
  outline: none;
}
.sort-box select:focus { border-color: #10b981; }

/* ── CATEGORY TABS ── */
.category-tabs {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  max-width: 1200px;
  margin: 1.5rem auto 0;
  padding: 0 2rem;
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  scrollbar-width: none;
}
.category-tabs::-webkit-scrollbar { display: none; }
.category-tab {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border-radius: 100px;
  border: 1px solid #e2e8f0;
  background: white;
  font-size: 0.9rem;
  font-weight: 600;
  color: #475569;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
  flex-shrink: 0;
}
.category-tab:hover { border-color: #cbd5e1; background: #f8fafc; color: #0f172a; }
.category-tab.active { background: #0f172a; color: white; border-color: #0f172a; }
.tab-count {
  background: #f1f5f9;
  padding: 2px 8px;
  border-radius: 100px;
  font-size: 0.75rem;
  font-weight: 700;
  color: #64748b;
}
.category-tab.active .tab-count { background: rgba(255,255,255,0.2); color: white; }

/* ── CARDS ── */
.hack-list-section { background: #fafafa; padding-bottom: 4rem; }
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
  gap: 1.5rem;
  padding: 2rem;
  max-width: 1200px;
  margin: 0 auto;
}
.hack-card {
  background: white;
  border-radius: 16px;
  border: 1px solid #e2e8f0;
  transition: all 0.25s ease;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05);
}
.hack-card:hover { transform: translateY(-4px); box-shadow: 0 20px 25px -5px rgba(0,0,0,0.1); border-color: #cbd5e1; }
.hack-card--past { opacity: 0.7; filter: grayscale(50%); background: #f8fafc; }
.hack-card--past:hover { opacity: 1; filter: grayscale(0%); }
.hack-card-body { padding: 1.5rem; flex: 1; display: flex; flex-direction: column; }
.hack-card-top-row { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 1rem; }

/* Source badges */
.source-badge { font-size: 0.7rem; font-weight: 800; padding: 4px 10px; border-radius: 6px; text-transform: uppercase; letter-spacing: 0.5px; }
.source-badge--devfolio   { background: #eef2ff; color: #4f46e5; border: 1px solid #c7d2fe; }
.source-badge--unstop     { background: #fffbeb; color: #d97706; border: 1px solid #fde68a; }
.source-badge--devpost    { background: #ecfdf5; color: #059669; border: 1px solid #a7f3d0; }
.source-badge--hackerearth { background: #fdf2f8; color: #db2777; border: 1px solid #fbcfe8; }
.source-badge--unknown    { background: #f1f5f9; color: #475569; border: 1px solid #e2e8f0; }

.status-badge { font-size: 0.7rem; font-weight: 800; padding: 4px 10px; border-radius: 6px; text-transform: uppercase; letter-spacing: 0.5px; background: #f1f5f9; color: #475569; border: 1px solid #e2e8f0; }
.status-badge--open { background: #ecfdf5; color: #059669; border-color: #a7f3d0; }

.college-badge { font-size: 0.7rem; font-weight: 800; padding: 4px 10px; border-radius: 6px; text-transform: uppercase; background: white; color: #0f172a; border: 1px solid #cbd5e1; }
.internship-badge { font-size: 0.7rem; font-weight: 800; padding: 4px 10px; border-radius: 6px; text-transform: uppercase; background: #f0fdf4; color: #16a34a; border: 1px solid #bbf7d0; }

.hack-card-title { font-size: 1.15rem; font-weight: 800; color: #0f172a; line-height: 1.3; margin-bottom: 0.75rem; letter-spacing: -0.3px; }
.hack-card-location { display: flex; align-items: center; gap: 6px; font-size: 0.85rem; color: #64748b; margin-bottom: 1rem; font-weight: 600; }
.hack-card-location svg { color: #94a3b8; flex-shrink: 0; }
.distance-label { color: #10b981; font-weight: 700; }

.hack-card-meta { display: flex; align-items: center; gap: 1.25rem; margin-bottom: 1rem; flex-wrap: wrap; }
.hack-card-meta-item { display: flex; align-items: center; gap: 6px; font-size: 0.85rem; color: #475569; font-weight: 600; }
.hack-card-meta-item svg { color: #94a3b8; flex-shrink: 0; }

.hack-card-tags { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 1.25rem; }
.hack-card-tags span { background: #f1f5f9; color: #475569; padding: 4px 10px; border-radius: 6px; font-size: 0.75rem; font-weight: 600; border: 1px solid #e2e8f0; }

.hack-card-footer { margin-top: auto; padding-top: 1.25rem; border-top: 1px solid #f1f5f9; }

/* ── ERROR / SKELETON / EMPTY ── */
.error-box { max-width: 600px; margin: 3rem auto; padding: 2rem; background: #fef2f2; border: 1px solid #fecaca; border-radius: 12px; text-align: center; color: #991b1b; }
.empty-state { text-align: center; padding: 5rem 2rem; color: #64748b; }
.empty-icon { font-size: 3rem; margin-bottom: 1rem; }
.skeleton {
  background: linear-gradient(90deg, #f8fafc 25%, #f1f5f9 50%, #f8fafc 75%);
  background-size: 200% 100%;
  animation: shimmer 1.5s infinite;
  border-radius: 16px;
  height: 280px;
  border: 1px solid #e2e8f0;
}
@keyframes shimmer { 0% { background-position: 200% 0; } 100% { background-position: -200% 0; } }

/* ── FOOTER ── */
.app-footer { border-top: 1px solid #e2e8f0; background: white; padding: 3rem 2rem; text-align: center; }
.footer-brand { display: flex; align-items: center; justify-content: center; gap: 10px; font-weight: 800; font-size: 1.2rem; color: #0f172a; margin-bottom: 1rem; }
.footer-tagline { color: #64748b; font-size: 0.95rem; margin-bottom: 1.5rem; }
.footer-links { display: flex; align-items: center; justify-content: center; gap: 1rem; font-size: 0.85rem; color: #94a3b8; font-weight: 600; flex-wrap: wrap; }
.footer-links a:hover { color: #0f172a; }
.footer-sep { color: #e2e8f0; }
.footer-about-btn { color: #475569 !important; font-size: 0.85rem; }
.footer-about-btn:hover { color: #10b981 !important; }

/* ── BTN LINK ── */
.btn-link {
  background: none;
  border: none;
  cursor: pointer;
  font-family: inherit;
  font-size: 0.9rem;
  font-weight: 600;
  color: #475569;
  padding: 6px 4px;
  transition: color 0.2s;
}
.btn-link:hover { color: #0f172a; }

/* ── LIST TABS (Upcoming / Missed switcher) ── */
.list-tabs {
  display: flex;
  align-items: center;
  gap: 0.5rem;
  max-width: 1200px;
  margin: 2rem auto 0;
  padding: 0 2rem;
  border-bottom: 2px solid #f1f5f9;
}
.list-tab {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 12px 24px;
  background: none;
  border: none;
  border-bottom: 3px solid transparent;
  margin-bottom: -2px;
  font-size: 0.95rem;
  font-weight: 700;
  color: #64748b;
  cursor: pointer;
  transition: all 0.2s;
  white-space: nowrap;
}
.list-tab:hover { color: #0f172a; }
.list-tab.active { color: #0f172a; border-bottom-color: #10b981; }
.list-tab .tab-count { background: #f1f5f9; padding: 2px 8px; border-radius: 100px; font-size: 0.75rem; font-weight: 700; color: #64748b; }
.list-tab.active .tab-count { background: #ecfdf5; color: #059669; }

/* ── PAGINATION ── */
.pagination {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  padding: 2.5rem 2rem;
  flex-wrap: wrap;
}
.pagination-btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 40px;
  height: 40px;
  padding: 0 14px;
  border: 1px solid #e2e8f0;
  border-radius: 8px;
  background: white;
  color: #475569;
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  transition: all 0.2s;
}
.pagination-btn:hover:not(:disabled) { border-color: #10b981; color: #10b981; background: #f0fdf4; }
.pagination-btn.active { background: #0f172a; color: white; border-color: #0f172a; }
.pagination-btn:disabled { opacity: 0.35; cursor: not-allowed; }
.pagination-ellipsis { color: #94a3b8; font-weight: 600; padding: 0 4px; line-height: 40px; }

/* ── ABOUT MODAL ── */
.modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(15, 23, 42, 0.55);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1rem;
  animation: fadeIn 0.2s ease;
}
@keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

.modal-window {
  position: relative;
  background: white;
  border-radius: 24px;
  box-shadow: 0 25px 60px -10px rgba(0,0,0,0.3), 0 0 0 1px rgba(255,255,255,0.1);
  max-width: 540px;
  width: 100%;
  max-height: 90vh;
  overflow-y: auto;
  animation: slideUp 0.25s ease;
}
@keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

.modal-close {
  position: absolute;
  top: 1rem;
  right: 1rem;
  background: rgba(255,255,255,0.15);
  border: 1.5px solid rgba(255,255,255,0.3);
  width: 34px;
  height: 34px;
  border-radius: 50%;
  font-size: 0.9rem;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  color: white;
  transition: all 0.2s;
  z-index: 10;
  backdrop-filter: blur(4px);
  font-weight: 700;
  line-height: 1;
}
.modal-close:hover { background: rgba(255,255,255,0.28); border-color: rgba(255,255,255,0.5); transform: scale(1.08); }

.modal-header {
  display: flex;
  align-items: center;
  gap: 1.25rem;
  padding: 2rem 2rem 1rem;
  background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
  border-radius: 24px 24px 0 0;
}
.modal-avatar {
  width: 56px;
  height: 56px;
  border-radius: 14px;
  background: linear-gradient(135deg, #10b981, #059669);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 1.25rem;
  font-weight: 900;
  color: white;
  flex-shrink: 0;
  letter-spacing: -1px;
}
.modal-name { font-size: 1.35rem; font-weight: 800; color: white; letter-spacing: -0.5px; margin-bottom: 4px; }
.modal-role { font-size: 0.85rem; color: #94a3b8; font-weight: 600; }

.modal-body { padding: 1.5rem 2rem 0; }
.modal-bio { font-size: 0.95rem; color: #475569; line-height: 1.7; margin-bottom: 1.25rem; }

.modal-badge-row { display: flex; flex-wrap: wrap; gap: 0.5rem; margin-bottom: 1.5rem; }
.modal-badge { padding: 6px 14px; border-radius: 100px; font-size: 0.8rem; font-weight: 700; }
.modal-badge--green { background: #ecfdf5; color: #059669; border: 1px solid #a7f3d0; }
.modal-badge--blue  { background: #eff6ff; color: #2563eb; border: 1px solid #bfdbfe; }

.modal-section { margin-bottom: 1.5rem; }
.modal-section h3 { font-size: 0.85rem; font-weight: 800; color: #0f172a; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 0.75rem; }
.modal-section p { font-size: 0.9rem; color: #475569; line-height: 1.65; }

.modal-tech-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0.5rem; }
.modal-tech-item { display: flex; align-items: center; gap: 8px; background: #f8fafc; border: 1px solid #e2e8f0; border-radius: 8px; padding: 8px 12px; font-size: 0.85rem; font-weight: 600; color: #334155; }

.modal-list { list-style: none; padding: 0; display: flex; flex-direction: column; gap: 0.5rem; }
.modal-list li { font-size: 0.9rem; color: #475569; line-height: 1.5; }

.modal-footer-note {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 10px;
  padding: 12px 16px;
  font-size: 0.85rem;
  color: #64748b;
  margin-bottom: 1rem;
  text-align: center;
}

/* ── Compact About Modal variant ── */
.about-modal-compact { max-height: 92vh; overflow: hidden; }
.about-body-compact  { padding: 1rem 1.5rem 1.25rem; overflow: hidden; }
.about-body-compact .modal-badge-row { margin-bottom: 0.75rem; }
.about-body-compact .modal-tech-grid { grid-template-columns: repeat(3, 1fr); gap: 0.4rem; }
.about-body-compact .modal-tech-item { padding: 6px 10px; font-size: 0.8rem; }
.about-body-compact .modal-section h3 { margin-bottom: 0.5rem; }

/* Chip row inside about */
.about-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
}
.about-chips span {
  background: #f1f5f9;
  border: 1px solid #e2e8f0;
  color: #475569;
  font-size: 0.78rem;
  font-weight: 700;
  padding: 4px 12px;
  border-radius: 100px;
}

/* Purple badge variant */
.modal-badge--purple { background: #f5f3ff; color: #7c3aed; border: 1px solid #ddd6fe; }


.modal-links { display: flex; justify-content: center; gap: 1rem; padding: 1.25rem 2rem 2rem; border-top: 1px solid #f1f5f9; margin-top: 0.5rem; }
.modal-link-btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 20px;
  border-radius: 8px;
  background: #0f172a;
  color: white;
  font-size: 0.875rem;
  font-weight: 700;
  transition: all 0.2s;
  border: none;
  cursor: pointer;
  text-decoration: none;
}
.modal-link-btn:hover { background: #1e293b; transform: translateY(-1px); }

/* ── H LOGO SVG ── */
.brand-h-svg { display: block; flex-shrink: 0; transition: transform 0.3s ease; }
.navbar-brand:hover .brand-h-svg { transform: rotate(-6deg) scale(1.07); }

@keyframes dotPulse {
  0%, 100% { opacity: 1; r: 1.5px; }
  50%       { opacity: 0.4; r: 1px; }
}
.brand-h-dot { animation: dotPulse 2.4s ease-in-out infinite; }

/* ── VIEW DETAILS BUTTON ── */
.view-details-btn {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 7px 16px;
  border-radius: 7px;
  background: transparent;
  border: 1.5px solid #10b981;
  color: #059669;
  font-size: 0.8rem;
  font-weight: 700;
  font-family: inherit;
  letter-spacing: 0.02em;
  text-decoration: none;
  transition: all 0.2s ease;
  cursor: pointer;
}
.view-details-btn:hover {
  background: #10b981;
  color: white;
  transform: translateY(-1px);
  box-shadow: 0 4px 12px rgba(16,185,129,0.25);
}
.hack-card--past .view-details-btn {
  border-color: #94a3b8;
  color: #94a3b8;
}
.hack-card--past .view-details-btn:hover {
  background: #94a3b8;
  color: white;
  box-shadow: none;
}

/* ── REGISTRATIONS META ITEM ── */
.hack-card-meta-regs { color: #6366f1; }

/* ── HIGHLIGHT CTA LINE ── */
.highlight-cta {
  margin-top: auto;
  padding-top: 1rem;
  font-size: 0.8rem;
  font-weight: 700;
  opacity: 0.6;
  letter-spacing: 0.03em;
}

/* ── DASHBOARD POPUP ── */
.dash-popup { max-width: 580px; }

.dash-header {
  background: linear-gradient(135deg, #0f172a 0%, #1e3a5f 100%);
}

.dash-metric-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 0.75rem;
  margin-bottom: 1.5rem;
}
.dash-metric {
  background: #f8fafc;
  border: 1px solid #e2e8f0;
  border-radius: 12px;
  padding: 1rem;
  text-align: center;
}
.dash-metric-val   { display: block; font-size: 1.75rem; font-weight: 900; color: #0f172a; letter-spacing: -1px; }
.dash-metric-label { display: block; font-size: 0.75rem; font-weight: 600; color: #94a3b8; margin-top: 2px; text-transform: uppercase; letter-spacing: 0.05em; }

/* Bar chart rows */
.dash-bar-row { display: flex; align-items: center; gap: 0.75rem; margin-bottom: 0.6rem; }
.dash-bar-label { font-size: 0.8rem; font-weight: 600; color: #475569; width: 52px; flex-shrink: 0; }
.dash-bar-track { flex: 1; height: 8px; background: #f1f5f9; border-radius: 100px; overflow: hidden; }
.dash-bar-fill  { height: 100%; border-radius: 100px; transition: width 0.6s ease; }
.dash-bar--online  { background: linear-gradient(90deg, #10b981, #34d399); }
.dash-bar--offline { background: linear-gradient(90deg, #6366f1, #818cf8); }
.dash-bar-count { font-size: 0.8rem; font-weight: 700; color: #334155; width: 30px; text-align: right; flex-shrink: 0; }

/* Source grid */
.dash-source-grid { display: flex; flex-wrap: wrap; gap: 0.75rem; }
.dash-source-item { display: flex; align-items: center; gap: 8px; }
.dash-source-count { font-size: 0.8rem; font-weight: 600; color: #64748b; }

/* code inside modal */
.modal-body code {
  font-family: 'Menlo', 'Monaco', monospace;
  font-size: 0.8rem;
  background: #f1f5f9;
  border: 1px solid #e2e8f0;
  border-radius: 4px;
  padding: 1px 5px;
  color: #0f172a;
}

/* ══════════════════════════════════════════════════════════
   RESPONSIVE — Tablet  (≤ 900px)
══════════════════════════════════════════════════════════ */
@media (max-width: 900px) {
  .hero {
    gap: 2.5rem;
    padding: 4rem 1.5rem 2.5rem;
  }

  .highlight-grid {
    grid-template-columns: 1fr;
    padding: 0 1.5rem;
  }

  .highlight-card h3 { font-size: 1.4rem; }
  .h-stat-val        { font-size: 2.25rem; }
}

/* ══════════════════════════════════════════════════════════
   RESPONSIVE — Mobile  (≤ 768px)
══════════════════════════════════════════════════════════ */
@media (max-width: 768px) {

  /* ── Navbar ── */
  .navbar { padding: 0 1.25rem; }
  .navbar-brand span { font-size: 1.05rem; }
  .mobile-menu-btn { display: block; }
  .navbar-links {
    display: none;
    flex-direction: column;
    align-items: stretch;
    position: absolute;
    top: 64px;
    left: 0;
    right: 0;
    background: white;
    padding: 1.25rem;
    border-bottom: 1px solid #e2e8f0;
    box-shadow: 0 10px 15px -3px rgba(0,0,0,0.07);
    gap: 0.5rem;
    z-index: 99;
  }
  .navbar-links.mobile-open { display: flex; }
  .navbar-links a {
    padding: 10px 12px;
    border-radius: 8px;
    font-size: 0.95rem;
  }
  .navbar-links a:hover,
  .navbar-links a.nav-active { background: #f8fafc; }
  .navbar-links a.nav-active { border-bottom: none; }
  .navbar-links .btn-primary,
  .navbar-links .btn-secondary { width: 100%; text-align: center; justify-content: center; }

  /* ── Search sticky offset adjusts when menu opens ── */
  .search-sort-container { top: 64px; }

  /* ── Hero ── */
  .hero {
    flex-direction: column;
    text-align: center;
    padding: 2.5rem 1.25rem 2rem;
    gap: 2rem;
  }
  .hero-content { width: 100%; }
  .hero-content h1 { font-size: 2.25rem; letter-spacing: -1px; }
  .hero-content p  { font-size: 1rem; margin: 0 auto 1.75rem; }
  .hero-cta-buttons { justify-content: center; }
  .hero-cta-buttons .btn-primary,
  .hero-cta-buttons .btn-secondary { flex: 1; min-width: 130px; max-width: 200px; text-align: center; }

  .hero-stats-panel { width: 100%; padding: 1.5rem; }
  .hero-stats-grid  { gap: 0.75rem; }
  .hero-stat-value  { font-size: 1.5rem; }

  /* ── Cold-start banner ── */
  .cold-start-banner { margin: 0.75rem 1.25rem; font-size: 0.85rem; }

  /* ── Logo strip ── */
  .logo-strip { padding: 1.5rem 1.25rem; }
  .logo-grid  { gap: 1.5rem; }
  .logo-item  { font-size: 1rem; }

  /* ── Section headers ── */
  .section-header { padding: 2.5rem 1.25rem 1.25rem; }
  .section-header h2 { font-size: 1.4rem; }

  /* ── About cards ── */
  .about-grid { grid-template-columns: 1fr; padding: 0 1.25rem; gap: 1rem; }
  .about-card { padding: 1.5rem; }

  /* ── Dashboard highlights ── */
  .highlight-grid   { padding: 0 1.25rem; gap: 1rem; }
  .highlight-card   { padding: 1.75rem; min-height: 220px; }
  .highlight-card h3 { font-size: 1.2rem; max-width: 100%; }
  .highlight-stats  { gap: 2rem; }
  .h-stat-val       { font-size: 2rem; }

  /* ── Search + sort ── */
  .search-sort-bar {
    flex-direction: column;
    align-items: stretch;
    padding: 0 1.25rem;
    gap: 0.75rem;
  }
  .search-box { min-width: 0; }
  .sort-box   { justify-content: space-between; }
  .sort-box select { flex: 1; }

  /* ── Category tabs — horizontal scroll ── */
  .category-tabs { padding: 0 1.25rem; margin-top: 1rem; }
  .category-tab  { padding: 8px 16px; font-size: 0.85rem; }

  /* ── Card grid — single column ── */
  .card-grid {
    grid-template-columns: 1fr;
    padding: 1.25rem;
    gap: 1rem;
  }

  /* ── Footer ── */
  .app-footer { padding: 2rem 1.25rem; }

  /* ── List tabs ── */
  .list-tabs { padding: 0 1.25rem; margin-top: 1.5rem; overflow-x: auto; -webkit-overflow-scrolling: touch; }
  .list-tab  { padding: 10px 18px; font-size: 0.875rem; flex-shrink: 0; }

  /* ── Pagination ── */
  .pagination { padding: 1.5rem 1.25rem; gap: 0.375rem; }
  .pagination-btn { min-width: 36px; height: 36px; font-size: 0.85rem; padding: 0 10px; }

  /* ── Navbar btn-link ── */
  .navbar-links .btn-link { padding: 10px 12px; border-radius: 8px; width: 100%; text-align: left; }
  .navbar-links .btn-link:hover { background: #f8fafc; }

  /* ── About modal — full-screen on mobile ── */
  .modal-window { max-height: 100vh; border-radius: 20px 20px 0 0; }
  .modal-backdrop { align-items: flex-end; padding: 0; }
  .modal-header  { padding: 1.5rem 1.5rem 1rem; border-radius: 20px 20px 0 0; }
  .modal-body    { padding: 1.25rem 1.5rem 0; }
  .modal-tech-grid { grid-template-columns: 1fr; }
  .modal-links   { padding: 1rem 1.5rem 1.5rem; }
}

/* ══════════════════════════════════════════════════════════
   RESPONSIVE — Small phones  (≤ 480px)
══════════════════════════════════════════════════════════ */
@media (max-width: 480px) {
  .navbar-brand span { display: none; } /* just show the H icon on tiny screens */

  .hero-content h1 { font-size: 1.9rem; }
  .hero-cta-buttons { flex-direction: column; }
  .hero-cta-buttons .btn-primary,
  .hero-cta-buttons .btn-secondary { max-width: 100%; width: 100%; }

  .hero-stats-grid { grid-template-columns: 1fr 1fr; gap: 0.5rem; }
  .hero-stat-box { padding: 1rem; }
  .hero-stat-value { font-size: 1.3rem; }

  .section-header h2 { font-size: 1.25rem; }

  .highlight-card { padding: 1.25rem; }
  .highlight-card h3 { font-size: 1.05rem; }
  .h-stat-val { font-size: 1.75rem; }
  .highlight-stats { gap: 1.5rem; }

  .hack-card-body { padding: 1.25rem; }
  .hack-card-title { font-size: 1rem; }

  /* ── Modal full screen on tiny phones ── */
  .modal-window  { border-radius: 16px 16px 0 0; }
  .modal-header  { padding: 1.25rem 1.25rem 0.75rem; gap: 1rem; }
  .modal-avatar  { width: 44px; height: 44px; font-size: 1rem; }
  .modal-name    { font-size: 1.15rem; }
  .modal-body    { padding: 1rem 1.25rem 0; }
  .modal-links   { padding: 0.75rem 1.25rem 1.25rem; }
}
